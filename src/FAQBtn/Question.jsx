import plusIcon from '../assets/images/icon-plus.svg'

const Question = ({setActive, isActive}) => {

    return (
        <button onClick={() => setActive()}></button>
    )
}

export default Question